<%= form_for(object, url: url, method: method) do |f| %>
  <%= f.label :threshold_quantity %>
  <%= f.text_field :threshold, value: object.threshold %>

  <%= f.label :discount_percent %>
  <%= f.text_field :percent, value: object.percent %>


  <%= f.submit %>

<% end %>
